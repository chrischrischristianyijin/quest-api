# requirements-curator.txt（CPU 版）
pip>=23.3
setuptools>=68
wheel

nemo-curator==0.7.1
crossfit==0.0.9

# 避免新大版本不兼容
pydantic>=1.10,<2
numpy>=1.24,<2
pandas>=1.5,<2
pyarrow>=10,<15

# 文本处理常用
trafilatura>=1.6
fasttext==0.9.2   # 需要系统依赖，见下
langid==1.1.6
regex

datasketch>=1.6.5

# 可并行
dask[complete]>=2023.9
